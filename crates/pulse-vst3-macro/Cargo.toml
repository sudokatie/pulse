[package]
name = "pulse-vst3-macro"
version = "0.1.0"
edition = "2021"
description = "Procedural macros for VST3 plugin export in Pulse"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full", "parsing"] }
